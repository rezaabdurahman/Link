# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=linkuser
DB_PASSWORD=linkpass
DB_NAME=linkdb

# Server Configuration
PORT=8080

# Search Service Integration (Feature Flag)
SEARCH_ENABLED=false
SEARCH_SERVICE_URL=http://search-svc:8080

# SEARCH_ENABLED Options:
# - false: Disable search integration, return unranked available users (default)
# - true: Enable search integration with search-svc for semantic ranking
#
# When SEARCH_ENABLED=true but search-svc is unavailable:
# - The system will gracefully fallback to unranked results
# - A warning message will be included in the API response
#
# The /available-users/search endpoint behavior:
# - With search disabled: Returns unranked list of available users
# - With search enabled and search-svc up: Returns semantically ranked results
# - With search enabled but search-svc down: Returns unranked list + warning
